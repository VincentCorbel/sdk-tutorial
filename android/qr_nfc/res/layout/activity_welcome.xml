<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:orientation="vertical"
    android:background="@drawable/bg">


    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:id="@+id/main_view"
            >
        <Button
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:text="@string/btn_start_activity_fg_only"
            android:gravity="center"
            android:layout_margin="25dp"
            android:paddingTop="18dp"
            android:paddingBottom="18dp"
            android:id="@+id/btn_start_activity_fg_only"
            android:background="@drawable/btn_bg"
            android:textColor="@color/white"/>

        <Button
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:text="@string/btn_start_activity_fg_bg"
            android:gravity="center"
            android:layout_margin="25dp"
            android:paddingLeft="12dip"
            android:paddingRight="12dip"
            android:paddingTop="18dp"
            android:paddingBottom="18dp"
            android:id="@+id/btn_start_activity_bg_fg"
            android:background="@drawable/btn_bg"
            android:textColor="@color/white"/>


        </LinearLayout>
    </ScrollView>

    <ImageView android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/ico_info"
            android:id="@+id/btn_information"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"/>

</RelativeLayout>
